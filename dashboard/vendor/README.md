# vendor

`mcp-http-client.js` is a byte-for-byte copy of `samples/lib/mcp-http-client.mjs` from
[mcp-broker](https://github.com/pandaGaume/mcp-broker) (Apache 2.0): the minimal MCP client
over Streamable HTTP that the broker samples use. The page imports it as an ES module.
