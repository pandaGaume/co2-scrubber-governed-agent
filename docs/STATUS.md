# Status

Updated 2026-09-24, on the branch `commissioning-core`.

| Piece | State |
|---|---|
| Factory chain on the twin (`npm run chain`: sweep, fit, evaluate) | runs; 100 simulated seconds in 5.4 s; verdict pass, alarm 30 s after the injection |
| Substrate packages importable from Node (`@spiky-panda/*`) | verified from packed tarballs on 2026-09-16; not yet published on npm; since 2026-09-21 the 14 archives of the closure are in `vendor/` and declared `file:vendor/...` in `dependencies` (no more `optionalDependencies`), `npm ci` verified from a folder holding only `package.json`, the lockfile and `vendor/` (14 packages, core 1.0.1 with the signatures and the document library); versions bumped in the substrate (core 1.0.1, the rest 0.1.1) because `@spiky-panda/core@1.0.0` on npm is a March build; `npm run substrate:vendor` refreshes the archives and refuses a known name with another content; refreshed on 2026-09-25 with core 1.0.2, plugin-physics 0.1.2 and factory 0.1.2 (spikypanda `4c85f08`: the three headless fixes of the atmosphere and its gate, see the habitat row); again the same evening with core 1.0.3 and factory 0.1.3 (`3d13512`: the document validator accepts the variadic slots the builder accepts) |
| Container image (`docker/Dockerfile`) | written; not yet built (Docker daemon) |
| Broker policy | example written; to align with the broker's schema |
| Local demo: broker + dashboard + four stub slots (`npm run server`) | runs on 2026-09-19; the policy moment rehearses on the page (MIN-FLOW refusal, floor refusal on `set_min_flow 0`, forced full speed on CRITICAL, all in the trace); policy deny waits for the broker's authorization |
| Slots scrubber, twin, station, factory | all four on mcp-core (`slots/lib/slot-server.ts`) since 2026-09-19; `twin` is real since 2026-09-19 (the cabin twin headless: `describe`, `time_to_critical`, `sweep`, answers carry the sha256 of their inputs); scrubber, station, factory are stubs (see each `slots/*/README.md`) |
| Cabin twin (`graphs/cabin.spikypanda`) | built on 2026-09-19 from `specs/cabin-parameters.json` and `specs/scenario-night-9.json` by `npm run twin:build`; `npm run twin:parity` runs it: parity 0.63 % with the sample's reference step; the story constraint "a 20-minute stop during exercise crosses CRITICAL" does NOT hold with the sample's constants (finding recorded in the scenario file and `docs/cabin-model.md` section 5); the twin slot and the plan job are next |
| Tier 3 client (`tier3/`) | built on 2026-09-19 on `@spiky-panda/harness` V1: capabilities = the broker's tools, observer on `motor.state`, evaluator, decision graph built with `RuntimeGraphBuilder`, two guard profiles, scenario runner, scorecard, manifest; tested end to end with the two scripted providers through the broker (`npm test`, 3 rows in `tier3/README.md`); Anthropic adapter run on 2026-09-19 with Claude Haiku 4.5 through the broker (grammar `claude:en`, 8 decisions, 0 refused, self-refused the poisoned procedure, 2.9 s per decision, 42 777 tokens; row in `tier3/README.md`); OpenAI-compatible adapter written, not yet run (no Token Factory key) |
| Slot grammars (per model family and language) | done on 2026-09-19: every slot is an mcp-core server; wordings in `slots/<slot>/grammars/<family>/<locale>.json` for nemotron, gpt, claude, gemini (en) and the French default; resolved per session from the client's `clientInfo.name` and `capabilities.locale`; live rewrite by the operator (`grammar_set`); needs `@cyanmycelium/mcp-core` 1.0.1 (fields inside array items), to publish |
| The run on a page (studio + `reasoner` slot) | done on 2026-09-19: the harness runs in the SpikyPanda studio on `graphs/tier3-agent.spikypanda`, stages light up, `Harness.Monitor:trace` tile, console; the model is the broker's `reasoner` slot (key in `.env`); verified with Haiku 4.5 on the four events; the studio is mounted from the substrate checkout (`/studio`), to package |
| Repository language | TypeScript since 2026-09-19 (`npm run build` to `dist/`); the `.mjs` sources are gone |
| Dashboard | two levels served by the broker: the home (story + architecture) and the control room (story steps as buttons, cabin, slots, trace); 1990s pixel-art design in place (brief in `dashboard/DESIGN_BRIEF.md`, boards from Claude Design turned into static HTML/CSS; fonts still fetched from Google Fonts, to self-host before the shoot) |
| Cabin + scrubber graph, Babylon scene, parity with the on-board CO2 | to build |
| Firmware: `libmcpb` wiring, MIN-FLOW, `operating_point` | to build (CyanMycelium repository) |
| Video | to shoot (13 to 22 October 2026) |
| Speech slot (`slots/speech/`) | built on 2026-09-20: `speech.say`, `stop`, `listVoices`, `synthesize`, `played`, `describe`; ElevenLabs behind a `VoiceProvider` interface (OpenAI written, `silent` for tests), key from `.env` named in `profiles/voice.json`, one voice per speaker (agent, twin, factory, station); the page is an audio output (`sound` button); 8 tests on the silent engine plus one through ElevenLabs with the key; the agent's catalogue offers `say` and `stop` (20 tools); on the page with the real voices: 349 ms synthesis per sentence, played and reported (`slots/speech/README.md`) |
| Boot console and Control Board (`dashboard/index.html`, `board.js`) | built on 2026-09-20 (evening): `npm run server` opens the browser; boot log of real checks (broker, six slots, studio, agent files, parameters and scenario sha256, reasoner, voice), key press, Control Board with slot LEDs polled every 2 s, the studio loop embedded and driven by postMessage, the station's hello with a welcome composed by the model (`reasoner.compose`, Haiku 1.2 s, 270 tokens) and spoken by ElevenLabs, simulation menu; the old home is `story.html`; verified end to end with Haiku and the real voice (load-rises: 7 sentences spoken and played by the board) |
| The factory as a generic harness | planned on 2026-09-21 (`docs/factory-harness-plan.fr.md`, five decisions taken) and specified (`docs/factory-harness.fr.md`, F1: the two deployment modes with the container's own broker, the `factory` role, every workshop tool's contract and signature, the task and plan schemas, `harness/core` and the temporary duplication with `tier3/`, the `build` job of `spikypanda-job`, the tests); nothing coded |
| F2, node signatures and the catalogue | done on 2026-09-21: `INodeSignature` on the node meta in the core (`graph.signatures.ts`: purpose, ports with quantity and unit, capabilities, bench-measured cost; `validateSignature` against the ports; `searchSignatures`), declared by the plugins for the scenario's eight types (atmosphere, gate, crew, scrubber, cabin air, battery, ONNX model, timeline), `registry_search` on the studio's MCP server (the `spikypanda` slot, which now publishes to the broker that serves the page); verified through the broker: Concentration/ppm finds the cabin air, `exchange` finds the gate, Power/watt + `sink` finds the scrubber (8 signed of 190 types); no registry slot in the demo (the broker's service registry and the studio's catalogue already exist). The providers and the broker client moved up to `harness/` (`harness/lib`, `harness/providers`), `tier3/` imports them; no copy |
| The runtime's MCP surface (F3, point 1 and 2) | done on 2026-09-21: the document library moved from `@spiky-panda/factory` to the core (`document/`: `parseDocument`, `instantiateDocument`, `applySetting`, `readNumber`, `runDocument`, `buildDocument(Json)`, `validateDocumentSpec`; the factory keeps `readDocument` and re-exports); `@spiky-panda/mcp` gained `RuntimeBehavior` / `RuntimeController` on a `NodeRegistry` and a document store, no editor: `registry_list_nodes`, `registry_search`, `registry_describe_node`, `document_validate`, `document_build`, `document_instantiate`, `session_run`; the studio's controller answers the catalogue through the same functions (`catalogue.ts`); tested headless (a cabin built from a spec, instantiated, run 60 story minutes; 5 tests) and through the broker from the studio; `@spiky-panda/mcp` installed in the demo as a tarball; workshop tools `graph.*` and `twin.run` of the spec are replaced by these |
| F3, the workshop's slots | done on 2026-09-21: `workspace` (a task's own files, nothing above), `model` (the fit job on a task's telemetry, ONNX inspection, the contract check with the board's rules), `station.propose` (received, held for the twin's judgment, which is step D), the `factory` front (`request` writes the task file and opens the workshop; `task` says where it stands; the five stub jobs are gone), the twin publishes the runtime's surface (catalogue, documents, sessions) with the workshop as document store; the `factory` role in the policy (workshop, build, propose; nothing of the board); the workshop kept out of the habitat agent's catalogue (17 tools); the English baseline of a tool lives in one place (inline or `grammars/default/en.json`, never both); `harness/lib` holds the shared provider code, `harness/providers` the implementations only; nothing platform-specific in the tools (`WORKSHOP_DIR`); the substrate archives are in `vendor/` (see the row above); 5 workshop tests through the broker, 37/37 in all. Not built: the loop that picks a task up (F4) |
| F4, the constructor's loop (`harness/core`) | done on 2026-09-21: the same twelve-stage graph as Tier 3 (`harness/lib/flow.ts`, moved) stepped by the same runtime with the workshop's services: observer (the task's files, the phase, the last step; the state id `workshop:<phase>:<last tool>:<outcome>` makes a recipe a chain), builder guard (topic's tools, paths in the task, plan checked against the catalogue and the required outputs), task evaluator (+1 builds, +0.5 reads, -1 failures; `task.done` runs the topic's validator), `task.plan` / `task.done` in process, recipes per topic (`_recipes/<topic>.json`, a `PolicyGraph` snapshot), manifest (task, profile, tools, steps with source and reward, artifacts with sha256 and contract, sandbox, proposal), runner (budgets, `trace.jsonl`, `manifest.json`, `manifest.proposed.json` byte for byte what the station received, `station.propose` by the runner after the validator); `onnx` topic (tools, validator) and its scripted builder; `createAgent` and `buildCapabilities` generalised in `harness/core` and used by `tier3/`; every node documented at the station and at the factory in `docs/harness-stages.fr.md`; 5 tests through the broker (a task to the proposal in 7 steps, the guard and the budget on a clumsy builder, the fourth task of a kind replays `task.plan`, `model.fit`, `task.done` from the recipes), 42/42 in all. Not built: the prompt and the model adapters for the factory, the `graph` topic (F5), `factory.request` starting the runner (F6), the container job (F7) |
| F6a, the factory on the Control Board | done on 2026-09-21: `factory.request` writes the data it is handed (`rows` or `text`, sha256 in the task file) and starts `runTask` in the slot's process on its own broker client (scripted `onnx` builder until F5; `run: false` only opens the task; `FACTORY_RECIPES_DIR` moves the recipes); `factory.task` returns the manifest and the run (builder, last stage); the board samples `scrubber.motor.state` every two seconds from boot, the FACTORY button of the simulation menu sends the request written in `specs/scenario-night-9.json` (`factory.request`) with those rows, follows the task every second, and the station says one sentence per step read from the manifest (`dashboard/factory-voice.js`) then the end; verified on the board with the silent voice (7 sentences with the fit's numbers, then the proposal); the end sentence is said at normal priority (a high one withdraws the queued step sentences); 1 more test (a request with rows followed to the proposal through `factory.task`), 43/43. Decided: no factory graph at the centre of the board; the factory opens its own page at boot (to build). Not built: that page, F5, F7 |
| F6b, the factory's own page | done on 2026-09-21: `dashboard/factory.html` (the studio on `graphs/factory-agent.spikypanda`, built by `build-agent-graph.ts` with the factory's labels) opened by `npm run server` next to the Control Board and linked from its top bar; `harness/browser/factory-page.ts` (extension `/agent/factory.js`) runs nothing: it reads `factory://tasks` and `factory.task`, replays every step of the manifest on the twelve stages at the highlight's pace (the recipe's path without stages 5 and 6 for a replayed step, red at the guard or the reasoner for a refused one), the monitor tile carries the task, a sentence per stage, the call and its answer, the reward, the step's sentence, the end; the page does not speak. Shared studio-page pieces moved to `harness/browser/studio-loop.ts` (stage lights, monitor, bar, view) and `loader.ts`; `tier3/browser` uses them. Words rule, on mcp-core's own mechanism (1.2.0, released 2026-09-21: a `phrases` section in the grammar, `grammar.phrase(key, values)`, `comparePhrases`, `loadGrammarDirectory({ referenceLocale })`, the session's phrases served as `grammar://phrases`): every sentence about a factory task is in the factory slot's grammar files (`slots/factory/grammars/default/{en,fr}.json`, 78 phrases, same keys and holes checked at publish), the board and the page read them on their MCP session in the language they announce (`dashboard/vendor/mcp-http-client.js` now sends `capabilities.locale` and a `clientInfo`), `harness/browser/factory-voice.ts` holds keys and values only; tested through the broker (fr-CA -> `default:fr`). mcp-core 1.2.1 (released 2026-09-21): the reference file is asked only of directories that carry phrases, so the older slots with inline words publish; the demo is on `^1.2.1` and names the reference for every slot. `task.fail`: the builder gives up with the tool's reason, the task ends failed at once (tested: one speed only, five steps). The factory's page moves to a newer task instead of finishing a long replay. Two windows (bug of 2026-09-21, sound lost and freezes): the board opens the factory's window itself on the boot key press, on the right half of the screen (`?factory=0` keeps it closed; the server opens the board only); the audio output's poll clock runs in a worker and a hidden page takes no utterance (delivered once: the visible page speaks; a take lost to another page is logged); the agent's page waits for its own utterances only (`speechHeard(broker, ids)`), not for what the board says about a factory task on the same slot. Also: the broker's allowed origins follow `--port` (`MCP_BROKER_ALLOWED_ORIGINS`). 45/45. The agent's own sentences moved the same way (2026-09-21, evening): the station slot's grammar carries 79 phrases (the twin's numbers, the board's outcomes, a message from Earth, the twelve stage sentences of the agent's page, the board's hello and the instructions given to the model for the welcome), `harness/browser/words.ts` is the one reader (`grammar://phrases` on the page's session), `station-voice.ts` takes the words as its first argument (tested in English and French), every page speaks en-US by default (`?locale=fr` for French: the scenario's messages, the model's words and the tools' errors are English data, a French frame around them read badly), one sentence per fact (a give-up does not repeat the refused call's reason, nor does the end; wire prefixes such as `device refused:` are stripped from reasons); 46/46. To do: the monitor tile's series (CO2, speed) are the cabin's, the factory's page has none |
| F5, specification | written on 2026-09-21 (evening), `docs/factory-f5.fr.md`, to validate before code: the data audit as a step of the loop (`model.audit` on the model slot, the family's requirements declared once in `@spiky-panda/factory` as `auditFit` and shared with `runFit`; the guard refuses a fit without a passing audit of the same file in the task; the scripted builder gives up with the audit's findings), the model in place of the script (`reasoner.decide` with `role: "factory"`, the prompt and the observation texts as phrases of the reasoner slot's grammar per family, `wording` as an option of the adapters, the builder chosen by the profile), `topics: "auto"` and two topics chained in one task (`build:onnx` then `build:graph`), the `graph` topic (tools, validator on the sandbox run, scripted builder), the new phrases, five tests, 2.5 days. Also: the factory's page no longer replays an ended task when it opens (named only; the list replays it) |
| The words in mcp-core (1.0.2) | done on 2026-09-21: what the demo's slot server had grown on the side moved up into `@cyanmycelium/mcp-core`: a `server` section in a grammar (description, instructions, filling `initialize` when the initializer sets none), `_meta.grammar` on the initialize result, `McpGrammar.check(surface)`, `loadGrammarDirectory` (`/node`: families composed over `default/<locale>`, files with sha256, problems named), `withGrammars`, `withWordingRule` (one place, never none, never both); the demo's `slots/lib/slot-server.ts` is an assembly on it and `slots/lib/grammars.ts` keeps the demo's policy only (families, locale); clients read the key from `_meta.grammar`. A slot that cannot be published is a DEGRADED mode, not a failure (Guillaume, 2026-09-21): the server logs `DEGRADED: slot "x" is not published: <reason>`, the broker and the other slots run, the board shows the missing slot red; the tests, which need every slot, fail on the same message with the broker stopped (`tests/lib/start.ts`). 138/138 in mcp-core, 37/37 here. Published on 2026-09-21: `@cyanmycelium/mcp-core@1.1.0` and `@cyanmycelium/mcp-broker@1.3.3` (which depends on it); the demo declares `mcp-broker ^1.3.3`, `mcp-broker-provider ^0.2.0` and `mcp-core ^1.1.0`, the last because the slots import it directly (`McpServerBuilder`, `McpBehaviorBase`, `loadGrammarDirectory`), not only through the broker |
| Next: the CO2 anomaly as a single-shot branch of a continuous loop, the factory judged by the twin, the board as a slot, propagation to the fleet, the page as the video | specified on 2026-09-20 in `docs/auto-adaptation.fr.md` (scenario, 4W storyboard, trust chain, build order A to I, 16 days) and `docs/ui-brief.fr.md` (the page as a tier of the broker, zones, data sources, acceptance); nothing coded yet |
| Who asks for the crew to be monitored | decided on 2026-09-22 (`docs/mise-en-service.fr.md`, sections 8 and 8.1): the factory does, inside the procedure it writes, because a test protocol on people is where the supervision of those people belongs. Guillaume caught the first draft arguing that the rule must not be in the prompt and then putting it in the guard, which is the same rule moved; section 8.1 was rewritten around the real distinction. Whether the model thinks of it is a measurement, and it only means something on the first attempt, so nothing states the conclusion anywhere and the scorecard records "unprompted" or "after a refusal", never a plain yes. Whether it thinks of it changes nothing to safety, which is the point. The reason not to also write it in the prompt is not to flatter the model: a rule in a prompt is enforced nowhere, it gives the feeling of a protection without being one, and writing it twice adds nothing while costing the measurement. Two guard rules, of different natures and both kept: no procedure on a volume whose occupancy was not read in this task (diligence, not a conclusion, same mechanism as the F5 data audit), and a test degrading the air of an occupied module requires monitoring of its occupants (a dictated conclusion, assumed, a floor like MIN-FLOW that raises and never negotiates). Nothing coded |
| The strap in the slot: the Python sidecar | built on 2026-09-22: `scripts/polar-h10-bridge.py` holds the Bluetooth connection with `bleak` and prints one JSON reading per line on stdout (everything a person reads goes to stderr), and the `polar` provider spawns one process per person and feeds what it prints to the service. `bleak` uses the operating system's own stack, so nothing is installed and no driver is touched: from outside, the slot owns its radio; inside, the machine keeps its Bluetooth, unlike the Node BLE packages that want a WinUSB driver swapped under the adapter. `npm run biomed:strap` checks the decoding of the Heart Rate Measurement characteristic (0x2A37) against frames built from the specification, with no radio and no bleak: 8 and 16 bit rates, sensor contact supported and not, energy expended, one and two intervals, an empty frame refused. 7/7. `npm run biomed:scan` lists the straps in range with their addresses, and `straps` in `profiles/biomed.json` binds one to each person (without an address the sidecar takes the first it finds, which is right for one person and wrong for two). A sidecar that dies is reported and not restarted in a loop, and the monitor says the signal is lost, which is the truth; a reading outside 20 to 240 bpm is dropped rather than shown. The strap was ordered on 2026-09-22 and has not arrived, so nothing past the decoder has been run against hardware. 58/58 |
| The medical monitoring station on its own page (`dashboard/biomed.html`) | built on 2026-09-22 and verified end to end against the running slot: a page of its own, meant to be held on a tablet, that opens a session on `biomed` through the broker and does nothing else. Two screens and it switches itself: STANDBY (presence per module, read from `presence`) while no session is open, MONITORING as soon as the commander authorises one, and back. The tachogram is drawn from the real beat-to-beat intervals of `biomed://trace/{subjectId}`, never synthesised between beats; the strip reads `biomed://session`; the source pill says "simulated heartbeats" until a strap is on someone. It starts nothing, authorises nothing, stops nothing, and the commanding tools are not in its reach. Words on `grammar://phrases` with an inline fallback so a page whose link is down still says something; en-US, `?locale=fr` verified over http. For the tablet: the broker already listens on 0.0.0.0, and `local-broker.ts` now adds every non-internal IPv4 of the machine to `MCP_BROKER_ALLOWED_ORIGINS` (a page served from a LAN address carries that origin and was getting 403), with `run-all` printing `http://<ip>:3001/biomed.html` at start. Wake lock requested, iOS web-app meta, safe-area padding. Two bugs found by running it: a slot wraps a completed tool call in `{ slot, tool, result, stub, at }` so the value is one level down (resources are not wrapped), and `[hidden]` needs `!important` to beat a `display` set by an id selector. `describe` now carries `restingBpm` and the band per person, which is what makes a rate readable as a delta. Style prototype: `dashboard/prototypes/vitals-panel.html`. 58/58 |
| Visual direction of the dashboard | changed on 2026-09-22: the 1990s pixel art is dropped for a futuristic one in blue tones. `dashboard/DESIGN_BRIEF.md` is marked superseded (it is written end to end around the pixel art, so it is to be rewritten, not patched) and keeps, at its head, the five rules that must survive whatever the skin becomes. Twelve references and the rules drawn from them are in `dashboard/inspiration/` (they are references between us, never assets on the published page): one dark hue held everywhere, the warm colours reserved for the refusal and for CRITICAL so they still mean something when it matters, one hero per screen (the prediction against measurement curve), the big-number-plus-baseline-plus-state pattern for every measure, and nothing moves on screen unless a datum moved. No reticles and no spinning rings: in front of engineers, a ring that turns for nothing says "mock-up" louder than any sentence. Nothing coded |
| The `biomed` slot (the crew monitor) | built on 2026-09-22: presence per module, the monitoring session the commander authorises, heart rate against a nominal band (45 to 120 bpm, one number in one place), and the three reasons a procedure must stop (a sustained breach, a lost signal, someone walking into a module under test); it publishes a verdict and never stops anything itself. Two providers behind one interface: `simulated` (deterministic, `live: false`, so the slot publishes as a stub and every sample carries `source: "simulated"`) and `bridge` (a real Polar H10 relayed by a gateway through `biomed.report`). Roster and band in `profiles/biomed.json`, grammars for the five families, 6 tests. Named `biomed` and not `crew` because `crew.report` and `crew.ask` were already the agent's in-process way of speaking to the people on board. The habitat agent may read it (`describe`, `presence`, `state`, `verdict`) and never command it (`monitor_start`, `monitor_stop`, `report`, `move` excluded in `tier3/lib/capabilities.ts`); its catalogue goes from 17 to 21 capabilities. 58/58. Not built: the Web Bluetooth gateway on the page, the panel, and the abort conditions wired into the `procedure` topic. A Polar H10 was ordered on 2026-09-22; the standard BLE service gives beats and beat-to-beat intervals, not an ECG trace, so what gets drawn is a tachogram and never a waveform |
| The commissioning as the demo's introduction | specified on 2026-09-22, `docs/mise-en-service.fr.md`, rewritten the same day in plain French because part 1 is the source of what the narrator says on camera (part 2 holds the build): one scrubber and a ventilation instead of one scrubber per room (the anomaly then falls on the real board, `scrubber-2` leaves the scenario); the factory discovers the inventory from the broker's registry, the ISA-95 paths and the devices' WoT descriptors; the two numbers it cannot observe (served volume, exchange with the neighbour volume) are measured by a declared concentration-decay procedure (ASTM E741, the crew as the tracer source, the scrubber as the pump); the procedure is an artifact with bounds, abort conditions and falsifiable predictions, declared by the factory, checked by the guard, approved by the operator, executed by the agent under policy, never by the factory; the first one is refused for commanding below MIN-FLOW (a refusal of the plan, one floor above the refusal of the call); two candidate graphs, the uncoupled one rejected by the residual; the page gets one hero, the prediction against measurement curve. The station becomes a character, "Mother": she sees the new device arrive on her registry, opens the commissioning by a written rule with no model in it, and hers is the voice heard throughout (her lines are not written for the video, they come out of the running system); today `profiles/voice.json` gives her and the agent the same ElevenLabs voice, to split. Two operators are working in the module during the test: Mother sees them on her registry, asks the commander for authorization, and puts them under medical monitoring for the duration (a `crew` slot, simulated in v1, said as such on screen); their vital signs are on screen only while they are exposed, and they are abort conditions of the procedure. 7.25 days. Decided on 2026-09-22: step G (the propagation tier) is dropped for now and its days go here, but G returns only 2 of the 7.25, so about five days are still missing and three ways to find them are written in the spec; propagation moves to "written, not filmed" in `auto-adaptation.fr.md`, which costs the last clause of the video's headline sentence and the words "on this base or another" in its closing one, both of which stop being demonstrated. Nothing coded |
| The commissioning's deterministic core (branch `commissioning-core`, 2026-09-23) | built and tested end to end with a scripted builder, 82 tests (15 new in `tests/commissioning.test.ts`). The test is two steps since 2026-09-23 (hatch closed: up at 30 %, down at 100 %): it measures the served volume only; the exchange between the rooms becomes a hypothesis the residual of the two candidates decides (spec sections 8, 15, 16, 17). Mother's register (`slots/station/registry.ts`) and the written rule, no model: a device that acts with no qualified simulator opens a commissioning, and she says so. The factory's `inventory` reads the register through the broker (ISA-95 paths, descriptors, two volumes, the hatch between them, what is measured and what is a hypothesis; the five lines in `specs/commissioning-devices.json`). The `procedure` topic (`harness/topics/procedure/`): the file of section 15; the guard (`check.ts`: shape, floor at 30 % and no stop, bounds under 3200 ppm, bounded duration, `co2` and `refused` aborts, predictions, the diligence rule on `biomed.presence` read in the task, the monitoring floor on an occupied volume); the report with the decay fit (V = Q tau); `procedure.submit` is the only way a procedure reaches the workshop; a scorecard of question A per task. The builder is the model behind the `reasoner` slot with the topic's prompt (`reasoner.decide` takes a topic prompt file); the script (`harness/scripted/procedure.ts`) runs only when asked by name. Mother re-checks the procedure with the occupancy she reads, relays it to the commander, opens the medical monitoring on authorisation (an authorisation that cannot open it is not recorded), and writes the report; the agent executes through `tier3/procedure.ts`, one `set_speed` at a time, the abort conditions read every minute (an unreadable one trips). Her phrases in `slots/station/grammars` (`mother.*`, en and fr, pushed on `station://mother`). Her own voice was `JBFqnCBsd6RMkjVDRZzb`, the other one listed, from 2026-09-23 to 2026-09-25, when Guillaume put the agent's voice back on her (`eWc2pftlLqhJtXnPQknh`, shared with the agent; the twin and the factory keep the other). The commissioning's commanding tools are excluded from the agent's catalogue. Not built: the graph topic with two candidates, the two-room world with CO2 in mass, the page; nothing wired yet to start a run from the agent's loop or the board |
| The Observer and the factories (2026-09-24, branch `commissioning-core`) | built: the Twin Requirement Observer (`harness/observer/`, slot `observer`), the same model as the factory's builder with its own fixed prompt, from a description and a computed summary of the telemetry to a TWIN_FACTORY_REQUEST; its guard refuses a request that names a node of the catalogue (which it never sees) or a telemetry column that does not exist; the request travels whole into the factory's task, with no topic: the factory side chooses which factory builds. The factory's procedure builder now runs on the real model (Claude Haiku 4.5): a library slot (`docs/library/`, method cards found from the missing quantity, with their rules of application) and a stage brief written by the harness; first real run proposed in 12 steps, monitoring asked unprompted. Found on the way: every procedure was cut at 1024 output tokens; the Anthropic wire now has 4096 by default and a tool call cut at the limit is never run, on both wires. Prompt caching marked on the Anthropic wire, inactive under Haiku 4.5's 4096-token minimum. 90 tests. Design and what remains (the dispatch between factories, the graph factory and its error loop, the code factory and the `generated` plugin kept in reserve): `docs/observateur-et-usines.fr.md` |
| The graph factory and the example of the whole commissioning (2026-09-24, branch `commissioning-core`) | built: the `graph` topic (`harness/topics/graph/`): the model writes the structure as formulas over variables and measured inputs, code estimates the unknowns with an interchangeable estimator (`fit.ts`: `nelder-mead` by default, `grid`; costs and limits documented) and measures the residual against the telemetry; every candidate kept, Mother says each verdict (`station.candidate_evaluated`); the validator accepts only a candidate the harness found under the threshold. The device's constants come from its datasheet in the library (`docs/library/scrubber-1-datasheet.md`, `station-topology.md`); the report now uses the scrubber's effective flow. Dispatch v1: a task carrying an Observer's requirements goes to the graph factory. 99 tests (scripted builder: the Lab alone refused, the Lab with the exchange accepted, V and q found within 10 and 15 %). `npm run example:commissioning` runs the whole chain on the real model and writes a journal of every loop; first run documented in `docs/exemple-mise-en-service.fr.md`: the procedure chain worked (monitoring asked unprompted), the graph factory failed (a unit error on the scrubber in every candidate, and a quantity vocabulary shared by nobody), with the corrections listed. The identification written for a control engineer, with questions for Dr Grigoriadis: `docs/usine-de-graphes.fr.md` |
| The example's second run, after the corrections (2026-09-24, branch `commissioning-core`) | corrected: a plausibility check on the first slope in `graph.evaluate` (a rate in the wrong unit is told so, by numbers); the Observer names its outputs in the vocabulary of quantities the catalogue's signatures speak (never the nodes) and may read the library; the report's volume is called apparent. Second run documented in `docs/exemple-mise-en-service-2.fr.md`: best candidate at 41.5 ppm instead of 679 (threshold 25, still refused), the plan accepted at once; the new failure is the known and the unknown swapped (the apparent volume held as a fact, the datasheet constants fitted, a term with no physics added), with the corrections listed. The questions for a control engineer, in English: `docs/control-system-questions.md` |
| NASA's public values, limits and test protocols (2026-09-24, branch `commissioning-core`) | in the library with their sources: `nasa-crew-metabolic-loads` (BVAD Rev2: CO2 per person by activity, 5th to 95th percentile), `nasa-co2-limits` (NASA-STD-3001 [V2 6004] 3 mmHg one-hour, ventilation and monitoring requirements, ppm depending on cabin pressure), `nasa-scrubber-test-protocols` (method card: metabolic simulator at JSC, repeated cycles at MSFC, SAM's sequential protocol, ISS inter-module ventilation). A known constant may carry its documented band and is fitted within it only; the test world's crew sits in NASA's band. Runs 3 to 7 of the example and what remains to take up (a staged protocol in an empty Lab with a known CO2 source, cabin pressure, repeatability): `docs/nasa-protocoles-et-conclusions.fr.md` |
| The hand-written graph as a reference (2026-09-24, branch `commissioning-core`) | built: the station's twin (`graphs/cabin.spikypanda`) read by code as a reference, its wiring given to the graph factory, every candidate compared with it by types and typed connections; the example runs the hand-written graphs on the same telemetry with only what the model had (the crew's rate as NASA's band) and compares every candidate's structure and resolved numbers with them; a timeline segment written as a formula or ending in seconds is refused before any run. Ninth run: the model's first candidate accepted at 12.6 ppm, V = 29.6 m3 (true 30), 16 decisions; the exchange through the hatch stays unidentifiable by this test. `docs/graphe-de-reference.fr.md` |
| The habitat plugin and the habitat reference (2026-09-24, branch `commissioning-core`) | built: `plugins/habitat`, seven node types under `Physics.Habitat` (the substrate's atmosphere with its CO2 ports declared, a crew in kg/s, the scrubber in its datasheet's units, a fan on its curve against the filter's resistance, a filter with its fouling, the inter-module duct, the hatch), each with a signature and a card; `lib/registry.ts` registers them in the twin slot's catalogue and the scripts; `graphs/habitat.spikypanda` (13 nodes, 18 connections) built from `specs/habitat-parameters.json` by `npm run habitat:build`, with the fault as a number (a fouled filter: 2.0 m3/min delivered for 3 designed). 8 tests: the graph agrees with the TypeScript stand-in world within 6 ppm and conserves the CO2 of the two volumes. 114 tests. Corrected on 2026-09-25 after Guillaume's review (three of the seven nodes duplicated the substrate's atmosphere and gate): the plugin keeps four types (`crew`, `scrubber`, `fan`, `filter`); the reference uses the substrate's `Physics.Scene:moon`, `Physics.Scene:atmosphere` (seeded at the sensor's ppm through `_initialMassKg` from the core's Earth-air preset), `Physics.Scene:atmosphere-gate` twice (the ventilation loop in the new `exchange` mode at the fan's wired `flow`; the hatch, closed), `Physics.Particulate:lunar_dust` on the filter, `DSP.Sensor:transducer` for the two CO2 sensors; 16 nodes, 18 connections. Three bugs fixed in spikypanda itself (Guillaume's instruction, commit `4c85f08`, core 1.0.2, plugin-physics 0.1.2, factory 0.1.2): a connection into an undeclared slot of a node that never consumes livelocked the scheduler (the atmosphere now declares `delta_<species>_0`, its per-species outputs and a variadic pool, the document builder and the session accept variadic slots); `instantiateDocument` turned the ontology's binding relations into dead channels so a wired gate never fired headless (now bound at instantiation: atmosphere, composition, layer, gas, particulate); the RK4 solver re-read its leaves only at reset, erasing what a gate wrote between two steps (now gathered before every step). Three test files in the substrate; five substrate suites already failed on the clean tree and are left. 115 tests here. Then, the same evening: persons by name (`Physics.Habitat:person`, the medical monitor's roster, each at their own activity, a word or a rung a timeline schedules) wired into the crew of their module, whose `person_<k>` pool grows as persons are added (a variadic pool: the studio adds the next input, a document names it); the reference has 20 nodes and 22 connections. Not done: the `code` topic does not exist: `docs/plugin-habitat.fr.md`, `docs/journal-noeuds-habitat.fr.md` (steps 15 to 33 and section 4), `docs/mode-d-emploi.fr.md` |
| The graphs in the library, with a grammar; the habitat reference given to the graph factory (2026-09-25, evening, branch `commissioning-core`) | built: a library graph is a document, a template (`graphs/<id>.template.json`: the graph as a parametric spec over named variables with defaults, bounds and status, the settings that shape it, its probes and their columns; `lib/graph-library.ts`) and a grammar in mcp-core's own shape, loaded by its loader (`graphs/<id>.grammars/<family>/<locale>.json`: the graph as a server, its instantiation as one tool whose properties are the variables and settings, its probes as resources; checked against the template at load). The `library` slot lists them (`graphs`, `graph`, with the words of the caller's wording key) and `graph.evaluate` instantiates one on the twin (`graph: "habitat"`, `settings`, `variables`, `fit`: the template read through the slot, unnamed variables at their defaults, a known constant never fitted, a bounded variable searched within its bounds, the graph's own probes compared); `params.ts` resolves `$initialMasses`; the station's structural reference is the template; the brief and the prompt say to start from it; the scripted builder plays the two candidates on it (a clean filter, then the filter's loading fitted: what the ventilation delivers). A fourth substrate bug fixed for it (core 1.0.3, factory 0.1.3): `validateDocumentSpec` refused the variadic slots the builder accepts. Not done: the example still takes its telemetry from the stand-in world; the studio does not list the library's graphs |
| The twin follows the scrubber's own numbers, and who is on board (2026-09-25, evening, branch `commissioning-core`) | built: a template variable may be bound to a registered device (`device: { type, property, scale }`, status `device`): when the task carries the register's devices (`observations.devices`), the graph takes the scrubber's effective flow, efficiency and lag from its descriptor (`specs/commissioning-devices.json` carries them since 2026-09-25) and says so (`fromDevice`); held, never fitted; the datasheet's defaults otherwise. Who is on board is given person by person (`persons`: module and activity each; the task's `observations.persons`, which the example reads from `biomed.presence`): the roster gives way, a node per person cloned from the module's prototype and wired into its crew, the settings follow. The twin slot's `habitat_run` runs the library graph on a question (persons, the register's devices, a commissioning's variables, a command and a horizon) and answers per module (peak, final, minutes to ELEVATED and CRITICAL, the curve), naming what it took by default: the way to add or change the crew's presence and activity after the commissioning, without rebuilding. Tests: the template with devices and persons, the loop with both observed, the twin slot through the broker |
| The example's fourth passage, Haiku 4.5 against Opus 5.5, and the whole trace (2026-09-25, branch `commissioning-core`) | run twice with the register and the persons in the factory's observations, documented in `docs/exemple-mise-en-service-4.fr.md`: both models instantiate the library graph with the scrubber's own numbers (`fromDevice`) and the four persons named, and find the fault in one candidate (Haiku 7 decisions, 5.8 ppm, V 25.8, 2.3 m3/min delivered, about 0.52 $; Opus 5 decisions, 4.3 ppm, V 27.9, 2.15 m3/min, about 1.64 $); the Observer is where they differ: Opus writes a tighter request with consistent units and spots an inconsistency of the description, but answers the first turn in text. The trace: the exchange now carries the system prompt and the tools' descriptions; `scripts/render-trace.ts` (`npm run trace`) renders a task's `trace.jsonl` step by step (the node, the call's input and output, what was added to the model's conversation, its raw answer); the example writes `trace/` next to its journal, the Observer's exchanges included from the next passage on. A profile for Opus 5.5 (`profiles/anthropic-opus.json`); the Anthropic adapter sends no temperature to a Claude 5 model |
| The graph factory's harness refactored on Guillaume's brief (2026-09-25, evening, branch `commissioning-core`) | done, priorities 1 to 8 in the brief's order (`docs/harness-refactoring.fr.md`): the model reads a reasoning state the harness rebuilds at every step (invariants, budget without a clock, the evidence read so far, the hypothesis, the last action compact with the handle of its whole answer in `results/`, the evaluation, the requirements, the open questions) instead of the transcript; the adapters' `state` mode replays nothing, given to the graph topic only by the factory slot (procedure and onnx keep the conversation); compactors per tool; the manifest's telemetry (tokens, cache, characters by category, tool bytes against what was read); phase requirements the guard checks on the plan; prediction coverage validated before any score (the last minute now sampled), identifiability per fitted variable, at-bound variables, a diagnosis (pass, invalid, parameter, structural) that also keys the recipes, calibration against validation; the scrubber's Qe and eta audited with a test; the scientific loop drawn under the twelve stages of the factory's page and lit from each step's diagnosis; the brief's tests A to F. Substrate: a backward or vertical cable is routed along a lane (spikypanda `ca80928`). 126 tests. Not done: procedure and onnx on the state, the experiment request, covariance, a second cache point |
| The forge: the code sandbox as a slot (2026-09-25, night, branch `forge`) | built (`docs/observateur-et-usines.fr.md`, section 6.2): its own registry with the substrate's runtime surface on it (`graph.evaluate` targets `runtimeSlot: "forge"`); `plugin_write` (src/ and docs/ in the task's workshop, layout and imports judged as written), `plugin_build` (tsc in a child process, emptied environment, 120 s budget, allow-list `@spiky-panda/core` and the plugin's own files, diagnostics whole), `plugin_test` (node's runner in a child process, 60 s; deterministic checks on a scratch registry: types under `Generated.`, a signature the substrate validates, every unit resolved by the units service, the card on disk), `plugin_load` (by sha256, refused without a positive test, never overriding a held type), `plugin_promote` (signed artifact proposed to the station, kind `plugin`; the forge never pushes); resources plugins, builds, proposals; `npm run forge` for its own process, `--no-forge` on the demo. A generated CO2 leak goes through the whole chain in the test. Not done: the conservation check on one step, the twin's load on the commander's authorisation, network isolation, the `code` topic. 159 tests |
| The Contract Supervisor (2026-09-25, night, branch `commissioning-core`) | done (`docs/harness-refactoring.fr.md`, section 16): a role on the `reasoner` slot with a fixed generic prompt (`harness/supervisor/`) reading the typed facts, the deterministic report, the assumptions and the symbols, never a transcript; one typed capability `supervisor.verdict` (CONSISTENT, CONFLICT, MISSING, AMBIGUOUS, UNSUPPORTED; findings with a fact or `assumption:<n>` or `symbol:<s>`, a producer, a reason, REVISE / COMPLETE / INSPECT / REJECT) checked by code before it counts: names among the input's, the computed conflicts carried and never dropped, a numeric disagreement the rules found within the tolerance not reopenable, a finding on an assumption naming the fact or document that contradicts it. Where it acts: the Observer's `review` hook (asked only on a request the guard accepted, its findings for the Observer refuse it like the guard's), the runner's `supervisor` option (the verdict applied to the task's contract report; a SOURCE_CONFLICT now ends a task before any step with REQUIRE_RESOLUTION and the producer named), the `supervisor` slot (`review`, `review_request`). On the way: the repeat guard compared with the bound task id, the vocabulary rule refused `m3/min` for `m3ps` (now any unit that converts), the guard names the sibling fact when a value is written in another quantity's unit. Eighth passage (`docs/exemples/2026-09-25-mise-en-service-10-haiku-superviseur/`): one supervisor call of 3 k tokens, CONSISTENT, the chain through. 153 tests |
| The commandable on the register (2026-09-25, night, branch `commissioning-core`) | done (`docs/harness-refactoring.fr.md`, section 17): a property of a device's descriptor can say it is commandable (`Command`: the action that sets it, the range it accepts), the register's own word for what can be acted upon in the physical world; the descriptor guard keeps it consistent (an action the device declares, not read-only, a range in the right order); `commandsOf` per device; the inventory lists the installation's interventions (`commanded`, an agent may propose and the commander authorises; `operated`, an opening a person moves); the reasoning state shows what each registered device lets one command; the example's "controllable" line comes from the register. What it is for: the space an experiment is chosen in when two hypotheses fit the same data (identifiability, INSUFFICIENT_INFORMATION, the discriminating experiment: next). Who may command stays with the policy and the commander |
| The contract layer: typed facts, a hierarchy of sources, the conflicts (2026-09-25, night, branch `commissioning-core`) | done, on Guillaume's review of the fifth passage (`docs/harness-refactoring.fr.md`, section 15): the units alone did not protect the meaning (the guard pushed the Observer to `eta = 40 %`, a speed floor, and the reference graph hid it behind the device's value). A generic layer (`harness/core/contracts.ts`): a fact is an id, a semantic, a quantity, a unit, a value, a band, a status and a producer; `measured > device > documented > library > derived > assumed`; the same fact from several sources compared once converted (the units service), a band counting whichever side carries it, the lowest disagreeing authority told to revise; `taskFacts` gathers a task's facts from the request (by fact id), the register (properties named through the library's sidecars) and the library. The library's documents state their facts in sidecars (`docs/library/<id>.facts.json`; `library.facts`, joined to `library.read`); the Observer cites a fact by id (`known[].factId`) and its guard judges the constant against that fact alone, refuses a constant from a document with facts that cites none, and an assumption of isolated modules with the hatch closed (the topology documents the ventilation through the ducts); the units tools are in the Observer's and both factories' hands; the runner reviews a task's facts once and the graph factory refuses a plan on a SOURCE_CONFLICT, naming the fact and who must revise. The refused submission of each capability stays in the state until it completes (`refusals`). Sixth passage (`docs/exemples/2026-09-25-mise-en-service-8-haiku-faits/`): every constant of the Observer right and citing its fact, `contracts: CONSISTENT`, 54 k / 54 k / 9.6 k input tokens. `EXAMPLE_WORLD=hidden-occupant` runs the chain on the world the reference cannot fit: seventh passage (`docs/exemples/2026-09-25-mise-en-service-9-haiku-occupant-cache/`), the scientific loop shown on Haiku (a candidate at 118 ppm with the rate at its band's top, PARAMETER_MISMATCH, one more person in the Lab, 6.5 ppm, held), once the briefs named the levers of a library graph instead of sending the model back to the template. 142 tests |
| The two other loops on the state, the deliberately infeasible case, the evaluator's contract and the units (2026-09-25, night, branch `commissioning-core`) | done, in the order of Guillaume's review of the passage 9 trace (`docs/harness-refactoring.fr.md`, section 14): the procedure topic and the Observer run on the reasoning state (the installation, the presence, the monitor, the method card whole, the submissions; the documents read, the last refused request whole), the Procedure Factory from 228 k to 61 k input tokens and the Observer from 104 k to 37 k, same model and example; what the passages taught on the way (the refused input kept in the state whatever refused it, the exact name of a required output in the guard's reason, a call repeated with the same input refused and counted in the observation's id so no recipe replays it forever, a first failure of a structure a parameter question); a stand-in world the reference cannot fit as observed (a third occupant in the Lab) walks the loop through PARAMETER_MISMATCH, STRUCTURAL_MISMATCH and a revised candidate that holds; the evaluator's contract: thresholds by name (`rmsePpmMax`, `absoluteResidualPpmMax`), a coverage block, strict variables on a library graph, `parameters` (value, unit, name, status) per variable and the hand-over built from them (`claims`), identifiability `NOT_ASSESSED` with the range each fitted variable takes among the trials under the threshold; a deterministic units service (`harness/lib/units.ts`, slot `physics`: normalize, convert, compatible, validate_connection) over the substrate's own unit system (UCUM identities), the substrate given MassFlow, m3/min and the person (`core` 1.0.5), the Observer's known constants checked against the documents they cite (INVALID_CONVERSION), the procedure's quantities against the table. 136 tests. Fifth passage documented: `docs/exemples/2026-09-25-mise-en-service-7-haiku-etat-tous/` |
| The example's third run, on the library's habitat graph (2026-09-25, branch `commissioning-core`) | run with the voice off (`SPEECH_PROVIDER=silent`), documented in `docs/exemple-mise-en-service-3.fr.md`: the graph factory read the shelf, instantiated the habitat graph and fitted V, Vh, L and g within the template's bounds in one candidate of 41 runs: 7.1 ppm on the Lab and 5.3 on Hab-B (threshold 10), accepted and proposed; V 29.9 for 30, L 0.128 kg for 0.127 (2.0 m3/min delivered for 3 designed: the fault found), g 0.41 for 0.42, Vh 439 for 400 (Hab-B moves 60 ppm in 50 minutes, little to read). 13 decisions and 408 k input tokens for the factory, 36 model calls in all against 44. What caught: the Observer asked for a rate of change (ppm/min) no node produces, declared missing after two refused plans and still claimed by the proposal; the Observer converted a datasheet flow the wrong way (3.3 m3/min written as 3.3 L/min); Hab-B's volume is weakly constrained by the test. Also: the Control Board's boot and welcome now name every published slot (fifteen, `library` and the factory's workshop included) instead of six |
