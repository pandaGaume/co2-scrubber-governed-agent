# Journal : les nœuds de l'habitat, écrits à la main, pas à pas

*Commencé le 24 septembre 2026 au soir. Guillaume demande un graphe de
l'habitat de référence (atmosphères, ventilation avec ventilateur et filtre,
épurateur, équipage, scène Lune), pour que l'usine adapte ensuite les
paramètres de l'épurateur et de l'installation au lieu de tout reconstruire.
Le substrat n'a pas les nœuds qu'il faut ; il faut les écrire. Ce journal
tient la trace de chaque action et du raisonnement qui la précède, dans le
format du journal de l'usine (but, outils, sortie), pour comparer ce
travail à celui qu'une usine de code aurait à faire
(`observateur-et-usines.fr.md`, section 6).*

**Conventions.** Une étape est une décision suivie d'une action. `lecture`
est l'équivalent de `workspace.read` ou `library.read` ; `catalogue`
l'équivalent de `twin.registry_describe_node` ; `écriture` n'a pas
d'équivalent aujourd'hui (c'est ce qu'une usine de code apporterait) ;
`essai` est l'équivalent de `graph.evaluate` ou d'un test.

---

## 0. La demande, et les décisions prises avant de commencer

Demande (Guillaume, 24 septembre, 18 h) : « lets generate a new graph for
that, with HVAC and filter and gate with the atmosphere node, gravity and
fans (later with motor) -> speed, debit... then we will use this model as
physical habitat reference where we gonna adapt the scrubber parameters
instead of building it from scratch ».

Trois questions posées avant d'agir, trois réponses :

| question | réponse |
|---|---|
| où écrire les nœuds manquants | un plugin local à ce dépôt, `Physics.Habitat`, enregistré par-dessus le registre du substrat ; spikypanda n'est pas touché |
| ce que l'usine ajuste sur l'habitat de référence | l'épurateur, l'installation (volume, débit délivré), l'équipage, et les **défauts** (encrassement des filtres, problème de moteur) |
| la gravité | la scène Lune, dite sans effet sur le CO2 brassé en V1 (elle servira au filtre à poussière plus tard) |

## 1. Les étapes

| n | but | action | ce qui en sort |
|---|---|---|---|
| 1 | savoir ce que le substrat offre déjà pour une atmosphère en masse | `catalogue` : liste des types enregistrés par les plugins (grep des identifiants dans `node_modules/@spiky-panda/plugin-*/dist`) | 74 types ; ceux qui comptent : `Physics.Scene:atmosphere` (masses par espèce N2, O2, CO2, H2O, Ar, volume, température ; sorties `ppm_CO2`, `mass_CO2`, `pressure`), `Physics.Scene:atmosphere-gate` (couple deux atmosphères ; modes `closed`, `open_passive`, `hvac_forced` à débit fixe), `Physics.Scene:moon` (gravité 1,625 m/s²), `Physics.Particulate:particulate` (descripteur seulement, V1). Rien pour un ventilateur, un filtre, un épurateur ou un équipage **en masse** : `Physics.LifeSupport:*` travaillent en ppm sur un volume replié |
| 2 | comprendre comment la porte lit et écrit une atmosphère, pour faire pareil | `lecture` : `plugin-physics/src/scene/atmosphere-gate.node.ts` (420 lignes) | la porte ne câble pas de données : elle se **lie** aux deux atmosphères par des liens de configuration (`atmosphere_A_in`, `atmosphere_B_in`, type `atmosphere`) et, à chaque `fire()`, applique des deltas de masse par espèce (`applyMassDelta`), en Euler explicite sur `session.dt`, signe opposé des deux côtés (conservation native). Le débit volumique transporte `flux_i = V̇ · P · x_i · M_i / (R T)` |
| 3 | savoir comment le constructeur de session reconnaît une porte, pour que mes nœuds soient reconnus sans toucher au substrat | `lecture` : `nodeeditor/dist/graph-session-builder.js` (lignes 455 à 650) | reconnaissance par **forme** (duck-typing) : un nœud qui a `bindAtmosphereA`, `bindAtmosphereB` et `clearBindings` est traité comme une porte ; un lien de configuration de type `atmosphere` vers le slot `atmosphere_A_in` ou `atmosphere_B_in` lui passe la poignée de l'atmosphère (`getMassKg`, `getMoleFraction`, `applyMassDelta`, `pressurePa`, `temperatureK`, `volume`). Tout autre couple type/slot est ignoré en silence. **Décision** : mes nœuds qui agissent sur une atmosphère prendront cette forme exacte |
| 4 | vérifier ce que l'atmosphère expose comme état et comme réglages | `lecture` : `atmosphere-layer.node.ts` (lignes 130 à 300) | éditables `volume` (m3), `temperature_k`, `initial_atmosphere_preset` (`issCabinECLSS` : CO2 à 0,3 %, soit 3 000 ppm, préréglage du substrat) ; masses par espèce ; 100 Hz demandés mais le pas de session reste celui du document |
| 5 | savoir comment un document construit sans éditeur relie ses nœuds, avant de choisir la forme des miens | `lecture` : `core/dist/document/document.builder.js`, `document.instantiate.js` (ce que l'usine, le slot `twin` et les scripts appellent) | deux règles qui tranchent : (a) le constructeur de document ne relie que des **ports déclarés** dans les métadonnées d'enregistrement (`node "x" has no input port "y"` sinon) ; les slots dynamiques de l'atmosphère (`delta_CO2_k`, `ppm_CO2`) ne sont donc pas câblables tels quels ; (b) `instantiateDocument` crée les canaux et attache le solveur, mais **ne fait aucune liaison de configuration** : personne n'appelle `bindAtmosphereA` hors de l'éditeur. Une porte liée par configuration tourne dans le studio, pas dans le bac à sable. **Décision, qui annule l'étape 3** : le couplage passe par des **signaux** (un conduit lit `ppmA`, `ppmB` et publie deux deltas de masse en kg/s), et l'atmosphère est une sous-classe qui déclare ses quatre entrées `delta_CO2_0..3` et ses sorties `ppm_CO2`, `mass_CO2` |
| 6 | savoir ce que coûte une boucle de signaux (atmosphère → épurateur → atmosphère) | `lecture` : `core/dist/sim/rk4-adaptive.solver.js`, `_snapshotInputs` | les entrées d'intégration sont prises une fois par macro-pas, par nom de slot de destination, sur les liens de type signal (les flux sont ignorés) : la boucle coûte un retard d'un pas (6 s) sur le terme de retrait, contre des constantes de temps de 30 min ; le cabin-air du substrat l'évitait en calculant le retrait chez lui, ici l'épurateur doit connaître la concentration. Accepté, et mesuré à l'étape 11 |
| 7 | savoir comment s'enregistre un type et ce qu'une signature doit tenir | `lecture` : `graph.registry.d.ts` (`INodeMeta`), `graph.signatures.js` (`validateSignature`, `searchSignatures`) | `register(type, factory, { label, category, docPath, inputPorts, outputPorts, signature })` ; une signature nomme des ports qui existent, avec une grandeur ; les unités ne sont pas vérifiées contre une table ; la recherche classe par sorties produites, puis capacités, puis coût, puis **ordre alphabétique** du type |
| 8 | savoir avec quelle copie du core mes nœuds tournent | `lecture` : `factory/bundle/spikypanda-factory.js` (1,8 Mo, webpack) | le bundle **embarque** sa copie du core et des plugins ; le plugin local importe `@spiky-panda/core` et `@spiky-panda/plugin-physics` de `node_modules`. Deux copies des mêmes classes coexistent ; le runtime reconnaît un nœud à sa forme (`fire`, `rhs`, `stateSize`), pas par `instanceof`. Vérifié à l'étape 11 ; un `instanceof` quelque part casserait tout |
| 9 | compiler des nœuds écrits comme ceux du substrat | `décision` : `tsconfig.json` gagne `experimentalDecorators` (les `@editable`, `@cloneable` du core sont des décorateurs legacy) et `plugins/**` ; rien d'autre du dépôt n'en utilisait | compile |
| 10 | écrire les nœuds | `écriture` : `plugins/habitat/signals.ts` (lecture des entrées, publication, densité du CO2), `atmosphere.node.ts` (sous-classe : ports déclarés, 1/6 Hz au lieu de 100, `initialCo2Ppm` posé après la graine du préréglage), `crew.node.ts` (L/min par activité × densité → kg/s), `scrubber.node.ts` (débit m3/s avec retard, rendement, retrait = η·Q·ρCO2(ppm), puissance), `fan.node.ts` (point de fonctionnement courbe/réseau, montée en vitesse), `filter.node.ts` (résistance = propre × (1 + charge/charge de doublement), charge intégrée), `duct.node.ts` (flux = Q·(ρA − ρB), deltas opposés ; `hatch` = même chose avec `open`), `index.ts` (enregistrement, signatures, fiches `docs/*.md`), `lib/registry.ts` (`buildRegistry` : le registre du substrat plus le plugin, branché dans le slot `twin` et les trois scripts) | 7 types, 8 fichiers, 1 100 lignes |
| 11 | vérifier que ça tourne avant d'écrire le graphe de référence | `essai` : un script jetable construit un document de 13 nœuds et 18 liens avec `buildDocumentJson`, l'instancie, le fait tourner 60 min à 6 s | tous les types résolus, aucun lien sauté (sauf solveur → scène, qui est une liaison de configuration, comme dans le graphe de la cabine) ; 600 pas en 31 ms ; le ventilateur délivre **2,001 m³/min** avec 0,127 kg de charge (3,0 propre) ; Hab-B à 3 ppm du monde de test ; le Lab jusqu'à 30 ppm au-dessous pendant la montée. Diagnostic : le monde de test démarre son épurateur de zéro ; avec `initialFlow = 0` les deux courbes sont à moins de 6 ppm sur 60 min. Le retard d'un pas (étape 6) vaut ce qui reste |
| 12 | faire du graphe une référence revue, pas un script | `écriture` : `specs/habitat-parameters.json` (chaque constante, sa source, son statut ; les vérités que le jumeau ne lit jamais y sont marquées), `lib/habitat.ts` (le spec du document depuis le fichier, la boucle de pas puisque le bundle n'exporte pas `runDocument`), `scripts/build-habitat-graph.ts` (`graphs/habitat.spikypanda` + manifeste : sha256 des deux fichiers, débit délivré et débit filtre propre), `tests/habitat.test.ts` (le registre, chaque nœud sur ses chiffres, le document contre le monde de test, la conservation du CO2 des deux volumes), `npm run habitat:build` | le document, son manifeste |
| 13 | juger | `essai` : `npm test` | 1 échec à moi (l'arithmétique du ventilateur dégradé : −15 % de débit, pas −20 %) ; 2 échecs de suites existantes : le catalogue classe désormais `Physics.Habitat:atmosphere` avant `cabin-air` pour « Concentration en ppm » (égalité, tranchée par l'ordre alphabétique, étape 7) ; le constructeur scripté ONNX demande depuis la capacité `cabin`, et le test de l'atelier accepte les deux types. **114 tests passent** |
| 14 | tenir la décision de nommage prise dans ce journal | `écriture` : les types renommés `Physics.Habitat:*` (une seule famille, comme décidé à la section 0, au lieu de `Habitat.Air` / `Habitat.HVAC`) | idem, 114 tests |

*Le 25 septembre au matin, Guillaume lit le résultat et corrige : « on avait
déjà un nœud atmosphère, avec une composition de gaz, inutile de dupliquer ;
pareil pour le hatch, qui est un item physique mais qui est modélisable par
AtmosphereInterface ; Particulate pour la particule ; on avait GasNode,
CompositionNode, AtmosphereNode et AtmosphereGateNode. So you missed a
lot. » Puis : les préréglages de scène du plugin physique sont à prendre ;
Chemistry n'est sans doute pas nécessaire, Atmosphere suffit ; ces nœuds
ont été testés en mode unitaire et certains jamais branchés sur un graphe,
il se peut que certains soient bugués ; et enfin : corriger directement
dans spikypanda, ça fera gagner du temps. Les étapes qui suivent sont
celles de cette reprise.*

| n | but | action | ce qui en sort |
|---|---|---|---|
| 15 | refaire l'inventaire avec la bonne question : non pas « qu'est-ce qui manque » mais « qu'est-ce que je refais qui existe » | `catalogue` + `lecture` : `Physics.Scene:*` (`moon`, `earth`, les préréglages d'atmosphère du core `ATMOSPHERE_PRESETS`), `Physics.Scene:atmosphere-gate` (modes `closed`, `open_passive`, `hvac_forced`), `Physics.Particulate:*` (`lunar_dust` et les autres descripteurs), `DSP.Sensor:transducer` (filtre, bruit, quantification, dérive), `Chemistry.*` (gaz, composition) | trois de mes sept nœuds étaient des doublons : `atmosphere` (une sous-classe pour déclarer des ports), `duct` et `hatch` (la porte refaite en signaux parce qu'elle ne tournait pas hors de l'éditeur). Le capteur de CO2 en ppm, que je posais en sonde, est un transducteur du substrat. La poussière que le filtre capte est un descripteur du substrat. Chemistry n'est pas nécessaire au graphe : l'atmosphère porte déjà sa composition. Décision : quatre nœuds restent (`crew`, `scrubber`, `fan`, `filter`), le reste est au substrat |
| 16 | semer un volume au ppm lu par le capteur sans sous-classe | `lecture` : `atmosphere-layer.node.ts` (`_initialMassKg`, cloneable, honoré au reset) ; `écriture` : `initialMassesKg(ppm, V, T, préréglage, P)` dans `lib/habitat.ts`, depuis `ATMOSPHERE_PRESETS`, `V1_SPECIES_ORDER`, `CHEMICAL_SPECIES_V1` et `GAS_CONSTANT_R` du core | un volume semé à 1 480 ppm relit 1 480 ppm, la pression gardée (test) |
| 17 | brancher l'équipage et l'épurateur sur l'atmosphère du substrat, comme avant sur la mienne | `essai` : `npm run habitat:build` | **la construction bloque** (arrêtée après dix minutes). Diagnostic par un script jetable qui corrige les ports de l'instance : un lien câblé vers un slot **non déclaré** d'un nœud qui ne consomme jamais (`delta_CO2_0` de l'atmosphère) devient un canal de flux que personne ne lit ; l'ordonnanceur dynamique attend son jeton sans fin. Premier bug, et la raison de ma sous-classe du 24 |
| 18 | corriger à la source : l'atmosphère déclare ses ports | `écriture` dans spikypanda : `atmosphere-layer.node.ts` (`delta_<espèce>_0` en entrée ; `mass_`, `mole_fraction_`, `partial_pressure_`, `ppm_<espèce>` en sortie, pour les cinq espèces), `scene/index.ts` (signature : MassFlow kg/s, Mass kg, Concentration ppm, Pressure Pa ; la réserve `delta_<espèce>_` déclarée variadique) ; `document.builder.ts` (un slot variadique est accepté et ajouté aux ports de la mise en page) ; `execution.session.ts` (`delta_CO2_1` hérite du genre de signal et du statut requis de `delta_CO2_0` : sans quoi jest lui-même bloquait) | le graphe se construit ; le test `atmosphere-headless-document` : une source sur `delta_CO2_1`, 5,94 g en 99 pas de 6 s |
| 19 | remplacer mon conduit et mon sas par la porte du substrat | `essai` : la porte câblée `atmosphere_out` vers `atmosphere_A_in` / `atmosphere_B_in` dans un document, mode `hvac_forced` | **la porte ne tire jamais** : `isAtmosphereAWired` reste faux. `lecture` : `document.instantiate.ts` transforme tout lien en canal de données, y compris les liens de configuration (relations de liaison de l'ontologie : `scene`, `solver`, `atmosphere`) ; seul l'éditeur (`graph-session-builder`) sait lier. Deuxième bug : c'est ce que l'étape 5 du 24 avait lu sans en tirer la conséquence |
| 20 | corriger à la source : lier à l'instanciation | `écriture` dans spikypanda : `document.instantiate.ts` (`bindConfigLink` : `atmosphere` vers `bindAtmosphereA` / `bindAtmosphereB` / `bindAtmosphere`, `composition`, `layer`, `gas`, `particulate` ; `clearBindings` une fois par cible ; le type du port de sortie lu dans les métadonnées du registre) ; `particulate/index.ts` et `chemistry/src/index.ts` enregistrent leurs relations dans `ONTOLOGY` ; `chemistry` ajouté à `fix:esm` du dépôt | la porte tire (test : liée des deux côtés, aucun lien sauté) |
| 21 | vérifier que le CO2 passe | `essai` : la porte liée à la main, 10 min | le ppm ne bouge pas. `lecture` : `rk4-adaptive.solver.ts` garde son propre vecteur `_y` et ne relit les feuilles qu'au `reseed` (reset) : ce que la porte écrit dans les masses pendant la phase de dispatch est **écrasé** au pas suivant par l'intégration du vecteur d'avant. Troisième bug, invisible dans l'éditeur tant qu'on regarde une seule atmosphère |
| 22 | corriger à la source : le solveur relit ses feuilles | `écriture` dans spikypanda : `rk4-adaptive.solver.ts` (`gatherState` de chaque feuille au début de `step`) ; test `solver-regather` (une rampe et une bosse écrite entre deux pas : 100, 210, 320) | le CO2 passe et se conserve |
| 23 | donner à la porte ce que le conduit faisait : un débit câblé, l'échange dans les deux sens | `écriture` dans spikypanda : `atmosphere-gate.node.ts` (entrée signal `flow`, qui prime sur `forcedFlow` ; mode `exchange` : les deux parcelles calculées sur l'état de début de pas, puis appliquées ; le débit compté dans `throughput`) ; `scene/index.ts` (port, signature VolumetricFlow m³/s) ; les fiches `atmosphere-gate.md` et `atmosphere.md` du plugin | test `atmosphere-gate-flow` (deux volumes de 30 m³ à 2 000 et 1 000 ppm, 2 m³/min chacun : la moitié du chemin en dix minutes, l'air net nul, le flux égal des deux côtés) |
| 24 | juger dans le substrat | `essai` : `npm test` de spikypanda, avant et après (`git stash`) | cinq suites échouaient déjà sur l'arbre propre (`dynamic` ×2, `sim-session`, `static`, `declared-units` : des unités orphelines `Voltage.V`, `Current.A` dans un nœud d'épurateur du substrat) ; laissées. Une garde que j'avais ajoutée au constructeur d'exécution (refuser un jeton vers un slot non déclaré) cassait trois suites de l'usine : la convention du dépôt est de laisser tomber les jetons sans destinataire dans les sous-graphes ; garde retirée, la correction reste celle de l'étape 18 |
| 25 | livrer | `écriture` : versions `core` 1.0.2, `plugin-physics` 0.1.2, `factory` 0.1.2 ; `npm run build:dev`, les quatre bundles ; commit `4c85f08` sur `main` de spikypanda ; `npm run substrate:vendor` ici (trois archives remplacées, les autres inchangées) | vendored |
| 26 | réduire le plugin | `écriture` : `atmosphere.node.ts`, `duct.node.ts` et leurs fiches supprimés ; `index.ts` à quatre types ; `filter.node.ts` prend un `particulate_in` ; `lib/registry.ts` n'augmente plus rien ; `lib/habitat.ts` : scène `moon`, atmosphères du substrat semées, deux portes (`exchange` au `flow` du ventilateur ; le sas `closed`), `lunar_dust`, deux transducteurs ; `specs/habitat-parameters.json` gagne le préréglage, la pression, les capteurs, la poussière, le sas | 16 nœuds, 18 liens ; 2,001 m³/min délivrés (3 propre) |
| 27 | juger | `essai` : `npm test` | deux échecs à moi (le test attendait quatre ports `delta_CO2_k` déclarés : il y en a un par espèce et une réserve variadique ; l'atelier attendait `Physics.Habitat:atmosphere` dans la recherche « Concentration en ppm » : c'est `Physics.Scene:atmosphere` qui y répond maintenant) ; **115 tests passent** |

*Le 25 septembre au soir, Guillaume demande, avant de donner la référence
à l'usine de graphes, des personnes individuelles qui peuvent avoir leur
propre activité, sur un nœud crew qui s'étend de lui-même à mesure qu'on
ajoute des gens ; puis que les graphes aillent dans la bibliothèque, pour
que le harnais choisisse quoi instancier sur le slot du jumeau ; donc que
le graphe vienne avec une grammaire, qui réutilise le cadre de mcp-core
pour décrire le graphe dans des termes qu'une IA comprend.*

| n | but | action | ce qui en sort |
|---|---|---|---|
| 28 | savoir comment l'éditeur étend une réserve d'entrées | `lecture` : `nodeeditor/src/variadic-reconciler.ts` (une réserve déclarée garde exactement une entrée vide en fin ; brancher la dernière en ajoute une), `node-def.ts` (`variadicInput` des métadonnées d'enregistrement) | déclarer `person_0` comme port et `variadicInput: [{ prefix: "person_" }]` suffit : le studio ajoute `person_1` quand `person_0` est pris ; le constructeur de document et la session acceptent les indices suivants depuis l'étape 18 |
| 29 | écrire la personne, et le crew extensible | `écriture` : `activity.ts` (l'échelle des activités, un mot ou un barreau 0 à 3 lisible depuis une timeline, les débits de référence de la NASA, la conversion litres-masse), `person.node.ts` (nom, indicatif, activité, ses quatre débits ; sorties `co2Delta`, `litresPerMinute`, `activityLevel`), `crew.node.ts` (la somme des `person_<k>` branchés plus l'effectif anonyme ; `headcount`), `index.ts` (cinq types ; la signature du crew nomme `person_0` et le pool), les fiches | compile ; le studio dessine les personnes, ses fiches à côté |
| 30 | mettre les quatre personnes dans la référence | `écriture` : `specs/habitat-parameters.json` (`crew.persons` : le roster de `profiles/biomed.json`, module et activité de chacun ; `crew.lab` et `crew.habB` retirés), `lib/habitat.ts` (un nœud par personne, branché sur `crew-<module>.person_<k>` ; les équipages à 0 anonyme) | 20 nœuds, 22 liens ; les mêmes ppm qu'avant (les débits sont les mêmes), le test du monde de test tient |
| 31 | mettre le graphe dans la bibliothèque, avec sa grammaire | `lecture` : `slots/tools/library/provider.ts` (les documents : titre, résumé, `**Measures:**`), `slots/lib/slot-server.ts` et `grammars.ts` (les grammaires mcp-core d'un slot : `loadGrammarDirectory`, une famille par-dessus `default/<langue>`, la clé résolue par session dans `_meta.grammar`), les types de mcp-core (`McpGrammar` : serveur, outils et propriétés, ressources, phrases) ; `écriture` : `lib/graph-library.ts` (un graphe = document + gabarit + grammaires ; `loadGraphLibrary`, `wordsOf` pour une clé, `instantiateTemplate` avec réglages et variables), `lib/habitat.ts` (`habitatTemplate` : les formules, les variables avec statut, les réglages, les sondes), `graphs/habitat.grammars/default/{en,fr}.json` (le graphe comme serveur, `tools.instantiate` avec les variables et réglages en propriétés, les sondes en ressources `probe://`), le slot `library` (`graphs`, `graph`, la ressource `library://graphs`, ses mots en et fr) | la grammaire est vérifiée contre le gabarit au chargement comme celle d'un slot contre ses outils ; une session `claude:fr` reçoit le français par défaut de la langue |
| 32 | donner la référence à l'usine de graphes | `écriture` : `params.ts` (`$initialMasses`), `evaluate.ts` (`graph: id` : le gabarit lu par le slot `library`, résolu avec les réglages, les variables non nommées à leur défaut, une constante connue jamais ajustée, une variable bornée cherchée dans ses bornes ; la référence de structure est le gabarit), `reference.ts` (le cadre est le préréglage de scène et le solveur, pas l'atmosphère), `index.ts` (schéma, brief, outils `library.graphs` et `graph`), `prompt.md`, la fiche `method-twin-graph`, le constructeur scripté (deux candidats sur `graph: "habitat"` : filtre propre, puis charge du filtre ajustée) | le constructeur scripté ne donne plus de spec |
| 34 | les nombres de l'épurateur sont ceux de l'appareil enregistré ; qui est à bord se donne personne par personne | `écriture` : `lib/graph-library.ts` (une variable liée à un appareil, `device: { type, property, scale }`, lue dans le descripteur du registre quand la tâche le porte ; `persons` remplace le roster, un nœud cloné du prototype du module et branché sur son équipage, les réglages qui suivent), `lib/habitat.ts` (`Qe`, `eta`, `lag` liés au `Scrubber` du registre ; les réglages nomment leur module), `specs/commissioning-devices.json` (le descripteur de l'épurateur porte son rendement et son retard), `evaluate.ts` (`persons`, `observations.devices`, `fromDevice` ; un nombre d'appareil n'est jamais ajusté), le constructeur scripté et l'exemple (`registry_list` et `biomed.presence` dans les observations), `slots/twin/habitat-twin.ts` et l'outil `habitat_run` du slot `twin` (le graphe sur une question : personnes, appareils, variables, commande, horizon ; par module le pic, le final, les minutes avant les seuils), ses mots en français | 13 tests sur le plugin, 16 sur l'usine et le jumeau |
| 33 | juger | `essai` : `node --test` | l'usine bute au `document_build` du jumeau : le **validateur** de documents du substrat ne connaît pas les réserves variadiques que le constructeur accepte (`person_1`, `delta_CO2_1` refusés). Quatrième bug du substrat, corrigé à la source (`validateDocumentSpec` accepte un slot d'une réserve déclarée ; test ; commit `3d13512`), `core` 1.0.3, `factory` 0.1.3, vendorisés. L'évaluation dit désormais les problèmes du validateur au lieu d'un refus plus loin |

## 2. Ce qui est sorti

- `plugins/habitat/` : cinq nœuds (`person`, `crew`, `scrubber`, `fan`, `filter`), leurs fiches (`docs/*.md`), l'enregistrement avec signatures ; `lib/registry.ts` les ajoute au registre du slot `twin` (le catalogue `twin.registry_list_nodes` les liste), des scripts et des tests. Le conteneur de l'usine (`spikypanda-job`) construit son propre registre et ne les a pas encore.
- Dans spikypanda (commit `4c85f08`, `core` 1.0.2, `plugin-physics` 0.1.2, `factory` 0.1.2) : trois bugs corrigés qui empêchaient une atmosphère et sa porte de tourner hors de l'éditeur (un lien vers un port non déclaré bloque l'ordonnanceur ; les liens de configuration devenaient des canaux morts ; le solveur écrasait ce qu'une porte écrivait), la porte avec un débit câblé et un mode `exchange`, trois fichiers de tests. Puis (`core` 1.0.3, `factory` 0.1.3) le validateur de documents qui refusait les réserves variadiques.
- `graphs/habitat.spikypanda` (20 nœuds, 22 liens), construit depuis `specs/habitat-parameters.json` par `npm run habitat:build` ; `graphs/habitat.manifest.json` ; `graphs/habitat.template.json` et `graphs/habitat.grammars/` : le même graphe dans la bibliothèque, avec ses mots, pour l'usine de graphes.
- Le défaut de la mise en service est un nombre du fichier : `ventilation.filter.initialLoadingKg = 0.127` ; le ventilateur délivre 2,0 m³/min pour 3 de conception.
- Le monde de test en TypeScript (`harness/stand-in/two-zone-world.ts`) et le graphe s'accordent à 6 ppm près : le monde peut être retiré quand l'exemple lira la référence.

## 3. Ce qu'une usine de code aurait à faire, comparé à ce travail

| ce que j'ai fait | avec quel outil | ce que l'usine a | ce qu'il lui faudrait |
|---|---|---|---|
| lire le code source de trois nœuds du substrat, du constructeur de session, du constructeur de document, du solveur (étapes 2 à 8 : environ 25 lectures) | l'accès au dépôt | `twin.registry_describe_node` (ports, unités, signature, chemin de la doc) ; pas le code | soit un outil `substrate.read` (les sources des plugins, le core), soit une fiche « comment écrire un nœud » dans la bibliothèque : la classe de base, les décorateurs, `setField`, la lecture des signaux, `rhs`, ce que le constructeur de document exige. La deuxième est moins chère et suffit aux nœuds simples |
| découvrir trois pièges par la lecture (ports déclarés seulement, pas de liaison hors éditeur, deux copies du core) | | rien ne le lui dit | les mêmes trois règles dans cette fiche, avec le message d'erreur qu'on obtient sinon |
| écrire huit fichiers, les enregistrer, les compiler | l'éditeur, `tsc` | rien : le sujet `code` n'existe pas (`factory-harness-plan.fr.md` : `code.write`, `code.test`, plus tard) | `code.write` dans `plugins/generated/`, `code.build` (tsc sur le plugin seul), l'enregistrement rechargé dans le registre du slot `twin` sans redémarrer |
| un essai de fumée qui a révélé un écart de 30 ppm et son diagnostic (l'état initial) | un script jetable, le monde de test | `graph.evaluate` (un résidu contre une télémétrie) | `code.test` : la même boucle de pas sur un document minimal, avec des sondes ; le résidu ne suffit pas, il faut lire les courbes et les termes (ce que `graph.evaluate` donne déjà : pente initiale, termes entrants) |
| des tests unitaires par nœud (le point de fonctionnement, le doublement de résistance, la conservation) | `node --test` | rien | l'usine écrit ses tests avec le nœud ; un nœud sans test ne rejoint pas le catalogue (`observateur-et-usines.fr.md`, section 6) |
| des fiches et des signatures avec les grandeurs partagées | | le vocabulaire des signatures | la même règle de garde que l'Observateur : une sortie nommée hors du vocabulaire est refusée |
| environ 25 lectures, 12 écritures, 6 essais, 2 heures | | un budget de 30 décisions par tâche | un budget par nœud (une sous-tâche par capacité manquante), pas un budget par graphe |

Le cas est gardé : ces quatre nœuds sont ce qu'une usine de code devra savoir produire, et ce journal est la trace à laquelle comparer son passage.

## 4. Ce que la correction du 25 septembre ajoute à la comparaison

- **L'inventaire avant l'écriture.** J'ai écrit trois nœuds qui existaient. La faute n'est pas dans la lecture (l'étape 1 avait vu l'atmosphère et la porte) mais dans la conclusion : devant un nœud du substrat qui ne tournait pas hors de l'éditeur, j'ai contourné au lieu de corriger. Une usine de code fera pareil si on ne lui interdit pas : la règle à écrire est « un doublon d'un type du catalogue est refusé ; si le type du catalogue ne tourne pas, c'est un bug à signaler, pas une raison de refaire ».
- **Les bugs sont dans le substrat, pas dans le plugin.** Trois sur trois. Des nœuds testés en unitaire et jamais branchés sur un graphe hors de l'éditeur : c'est exactement ce que Guillaume avait prévenu. Un `code.test` de l'usine doit donc faire tourner un document minimal avec les nœuds du substrat qu'il touche, pas seulement le nouveau nœud.
- **Corriger le substrat coûte moins que le contourner.** Les trois corrections tiennent en quelques dizaines de lignes chacune et rendent les nœuds utilisables par tout le monde ; mes trois doublons faisaient mille lignes et ne servaient qu'ici. Mais cela suppose l'accès au dépôt du substrat, que l'usine n'a pas : quand elle bute sur un nœud du catalogue, sa sortie est un rapport de bug (le document minimal qui échoue, la lecture qui explique), pas un contournement.
- **Ce que le test d'un nœud doit contenir**, tiré des trois bugs : un producteur câblé sur chaque port déclaré (le blocage), un lien de configuration dans un document construit sans éditeur (la liaison), deux pas de solveur avec une écriture entre les deux (l'écrasement).
