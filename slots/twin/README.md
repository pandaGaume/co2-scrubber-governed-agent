# slot `twin`

Tier 0, the oracle. The SpikyPanda graph of the cabin, the crew, the turbine and the DC motor, running in a browser tab with a Babylon.js scene, published into a broker slot the way the node editor already is. It answers the agent's questions (a `sweep` of a few points under a budget) and is the same graph the factory runs headless.

Status on 2026-09-16: the RS-385 montage (`graphs/rs385-complete.spikypanda`) stands in for the cabin graph; the cabin + crew + scrubber graph and the Babylon scene are to build; a parity test against the on-board CO2 dynamics is required.
