{
    "tools": {
        "request": {
            "description": "Ask the factory for a missing capability. Give the functional contract: required outputs (name, quantity, unit), constraints, observations, data files. Returns taskId. Never give a solution.",
            "properties": {
                "objective": "object: required_outputs [{name, quantity, unit?, horizonMinutes?}], constraints {}",
                "observations": "object: what was observed",
                "data": "array of {file, columns?, sha256?}",
                "topics": "[\"graph\", \"onnx\"] or \"auto\"",
                "budget": "object: iterations, minutes, twinPoints",
                "requestedBy": "string: tier3 | operator",
                "profile": "string: profile file"
            }
        },
        "task": {
            "description": "State of a task by taskId: created | running | proposed | accepted | rejected | failed, workspace, taskSha256, files, manifest.",
            "properties": {
                "taskId": "string, from request"
            }
        }
    }
}
