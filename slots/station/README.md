# slot `station`

Tier 2. The station of `@spiky-panda/applications-motorwatch`: validated model push (sha256, input/output contract, double bank), the regime catalogue, and the journal of stable operating points (`ISteadyStateJournal`) fed by `operating_point` notifications, which a `fit` reads unchanged. Registration of a new artifact requires a positive `evaluate` report id.

Status on 2026-09-16: the station and the journal exist in the substrate; exposing them as a broker slot with the registration rule is to build.
